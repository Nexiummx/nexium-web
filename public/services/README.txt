Las vistas previa de servicios son **UI en código** (componentes en `components/services/ServiceMockup.tsx`), sin imágenes estáticas.

Para ajustar layout o copy, edita ese archivo o los subcomponentes `MockupWeb`, `MockupEcommerce`, `MockupAutomations`, `MockupConsulting`.
